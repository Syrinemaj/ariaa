fastapi==0.111.0
uvicorn[standard]==0.30.1

sqlalchemy==2.0.31
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.2

redis==5.0.7
celery==5.4.0

pydantic==2.8.2
pydantic-settings==2.3.4

python-multipart==0.0.9

httpx==0.27.0

openai==1.42.0

# Amazon Bedrock (Converse API — Claude, Nova, ...) — comparaison de
# modèles pour la migration Groq -> Bedrock (voir app/ai/bedrock_client.py).
# AWS credentials via boto3 standard (env vars / ~/.aws/credentials / IAM
# role). Pas le SDK `anthropic` : son client Bedrock (Mantle) renvoyait
# 404 sur ce compte pour tout ID testé ; boto3.converse() natif fonctionne.
boto3==1.43.51

# Local embeddings (all-MiniLM-L6-v2, 384 dims) — no API key, no cost
sentence-transformers>=2.7.0
# CPU-only torch is sufficient; GPU is detected automatically if available
torch>=2.0.0

pgvector==0.2.5

pandas==2.2.2
openpyxl==3.1.5

python-jose[cryptography]==3.3.0
bcrypt==3.2.2
passlib[bcrypt]==1.7.4
email-validator==2.2.0
slowapi==0.1.9
jsonschema==4.23.0

structlog==24.4.0

opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
opentelemetry-exporter-otlp==1.27.0
prometheus-client==0.20.0
